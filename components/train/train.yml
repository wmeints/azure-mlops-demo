$schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json
name: train
version: 1
description: Trains the model
display_name: Train model
type: command
inputs:
  training_data:
    type: uri_file
outputs:
  model:
    type: mlflow_model
code: .
environment: azureml:infosupport-mlops-ebm:5
command: >-
  python component.py --training-data ${{inputs.training_data}} --model-file ${{outputs.model}}